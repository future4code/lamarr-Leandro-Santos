``` javascript 
function calculaPrecoTotal(quantidade) {
    let macas = quantidade * 1.30
    
    if(quantidade < 12){
      macas
    } else{
      macas = quantidade * 1
    }
     
  return macas
}
``` 